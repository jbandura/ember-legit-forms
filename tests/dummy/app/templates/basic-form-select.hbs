<div class="container">
  {{#lf-form rules=rules as |v formValid|}}
    {{lf-input
      label="Name"
      property=model.name
      name="name"
      validate=v
      class="js-name"
      inputClass="fooClass"
      on-update=(action (mut model.name))
    }}
    {{lf-input
      label="Second Name"
      property=model.name
      name="secondName"
      validate=v
      class="js-second-name"
      on-update=(action (mut model.secondName))
    }}
    {{lf-select
      property=model.group
      name="group"
      label="Group"
      content=groups
      validate=v
      class="js-group"
      inputClass="fooClass"
      on-update=(action (mut model.group))
    }}
    {{lf-select
      property=model.group
      name="subgroup"
      label="Subgroup"
      content=groups
      validate=v
      class="js-sub-group"
      on-update=(action (mut model.subGroup))
    }}
    {{lf-textarea
      property=model.bio
      name="bio"
      validate=v
      label="Bio"
      class="js-bio"
      on-update=(action (mut model.bio))
    }}
    {{lf-textarea
      property=model.description
      name="description"
      validate=v
      label="Description"
      class="js-description"
      inputClass="fooClass"
      on-update=(action (mut model.description))
    }}

    <button class="js-submit btn btn-primary" disabled={{not formValid}}>Submit</button>
  {{/lf-form}}
  <button class="js-clear btn btn-default" {{action "clear"}}>Clear</button>
</div>
